# Скопируйте этот файл в .env и вставьте свои значения
# .env НЕ попадает в git (см. .gitignore) — это защита секретов

# Токен от @BotFather
BOT_TOKEN=вставьте_сюда_токен_от_botfather

# Ссылка для чаевых (Ozon Банк). Не публикуется в git.
# Заполняется в .env на сервере.
DONATE_URL=
